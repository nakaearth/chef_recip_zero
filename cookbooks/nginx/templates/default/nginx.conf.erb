user nginx;
worker_processes 1;
error_log /var/log/nginx/error.log;
pid /var/runfault_type application/octet-stream;
  server {
    listen <%= node['nginx']['port'] %>;
    server_name localhost;
    location / {
      root /usr/share/nginx/html;
      index index.html index.htm;
    }
  }
}
